#include "s_rune.h"
#include "video_feeder.h"
#include <string>

int main(void) {
    /*
    string file_path = "fire.mp4";
    CameraBase * cam = new VideoFeed(file_path);
    int code = cv::VideoWriter::fourcc('M', 'J', 'P', 'G');
    cv::VideoWriter save_stream("fire_rune_detect.avi", code, 30, Size(640, 360));
    s_rune rune_getter;
    while (cam->is_alive()) {
        Mat detected = rune_getter.fire_get_res(cam);
        save_stream << detected;
        cv::imshow("fire_detect", detected);
        cv::waitKey(1);
    }
    save_stream.release();
    */
    return 0;
}
